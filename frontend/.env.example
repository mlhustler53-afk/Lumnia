# Backend API URL (required in production when frontend and backend are on different hosts)
# Leave empty in local dev — Vite proxies /api to http://localhost:3001
# VITE_API_URL=https://your-backend.example.com
